struct s {
    a: usize,
    b: uint8,
    c: uint8,
    d: uint8,
}

function main() -> usize {
    let a: s = s { a: 1, b: 2, c: 3, d: 4 };
    let b: s = a;

    if (b.a != 1) return 1;
    if (b.b != 2) return 2;
    if (b.c != 3) return 3;
    if (b.d != 4) return 4;
    return 0;
}
